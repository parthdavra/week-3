[project]
name = "session"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "docker>=7.1.0",
    "jupyter>=1.1.1",
    "mlflow>=3.12.0",
    "notebook>=7.5.6",
    "scikit-learn>=1.8.0",
]
